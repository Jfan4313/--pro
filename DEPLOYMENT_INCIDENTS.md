# 公网部署故障台账与发布 SOP

> 文档类型：部署故障记录 / 发布统筹单  
> 服务：智建协同 Pro  
> 公网地址：https://project.zero-carbon.online  
> 当前状态：服务已恢复，公网仍运行旧版本，待部署脚本修复发布后验收

## 一、问题摘要

| 项目 | 记录 |
|---|---|
| 问题 | 公网部署在服务器端反复失败，导致新版本无法切换 |
| 影响 | AI 调试、语音转文字和手机端录音按钮修复尚未确认在公网生效 |
| 当前公网版本 | `buildSha=6b3c6b36c91c6c5d855de306664e03e7a0642ef0` |
| 目标版本 | `ed35063` 及后续部署脚本修复提交 |
| 服务器运行时 | Node.js `v20.20.2`、npm `10.8.2` |
| CI 构建运行时 | Node.js 20/24 Runner |
| 主要根因 | 服务器磁盘空间不足；服务器 Node.js 与依赖要求不一致；发布包依赖处理策略不稳定 |
| 风险等级 | 高：发布失败会持续保留旧版本，且重复发布会继续消耗服务器空间 |

## 二、已确认的失败记录

| GitHub Actions Run | 结果 | 失败阶段 | 证据 / 根因 |
|---|---|---|---|
| `32256765665` | 失败 | 服务器依赖重建 | `better-sqlite3` 在发布目录无法创建 `build` 目录；CI 的 Node 依赖与服务器 Node 版本不一致 |
| `32257162628` | 取消 | 服务器依赖安装 | `npm install` 长时间未完成，后续取消 |
| `32257654321` | 失败 | 服务器依赖安装 | `npm WARN EBADENGINE`，大量依赖要求 Node.js >=20；最终 `ENOSPC: no space left on device` |
| `32258080950` | 失败 | 服务器依赖安装 | 仍因 `ENOSPC` 写入失败；说明服务器可用磁盘空间未恢复 |
| `32265527272` | 超时取消 | 服务器依赖安装 / SSH | 旧部署入口执行无超时的 `npm install`；随后生产服务因复用 Node 18 编译的 `better-sqlite3` 报 `libnode.so.109` 缺失并反复重启 |

## 三、已做的部署流程改动

- 不再把 CI 环境的 `node_modules` 打入发布包，避免把 Node 20 编译产物带到 Node 18 服务器。
- 尝试在服务器端按生产环境安装依赖。
- 增加发布临时目录清理，避免残留 stage 目录持续占用空间。
- 尝试在 lock 文件一致时复用服务器依赖，并改用硬链接降低 staging 磁盘占用。
- 服务器已升级到 Node.js 20，并在生产目录重新编译 `better-sqlite3`；本机和公网健康接口均已恢复，当前仍运行 `6b3c6b36...`。
- 部署脚本在复用依赖时会单独复制并重编译 `better-sqlite3`，避免旧 Node ABI 原生二进制再次进入新版本。
- 服务器依赖安装和原生模块重编译各增加 10 分钟超时，GitHub SSH 部署增加 12 分钟总超时和连接保活。
- 新脚本尚未完成生产发布验收；公网 `buildSha` 切换到最新提交前不能视为全部解决。

## 四、统一整改方案

### P0：服务器环境恢复

1. 在服务器上确认磁盘使用情况：`df -h`、`du -xh /opt /var/lib/zhijian-pro | sort -h`。
2. 清理可确认的发布临时目录：`/opt/zhijian-pro-stage.*`。
3. 清理 npm 缓存和已确认无用的失败发布备份；保留最近一个可回滚版本和生产数据。
4. 将服务器 Node.js 升级到 Node.js 20 LTS，与 CI 构建环境统一。
5. 确认 `/var/lib/zhijian-pro`、`/opt/zhijian-pro` 和 systemd 服务仍可读写。

### P1：发布策略统一

1. 服务器统一使用 Node.js 20；lock 文件不同时执行 `npm ci --omit=dev`，相同时可复用 JavaScript 依赖，但必须在 staging 内独立重编译原生模块。
2. 发布前增加磁盘预检；可用空间不足时直接失败，不进入服务切换。
3. 发布包不携带 `node_modules`；服务器安装成功后才允许替换应用目录。
4. 对 `better-sqlite3` 做一次服务器本机启动验证，再执行生产切换。
5. 服务器只保留上一版可运行副本，完整版本历史由 GitHub 保存，避免备份目录无限增长。

部署脚本现在会在解包前执行两项硬性预检：`/opt` 至少保留 1 GiB 可用空间，服务器 Node.js 必须为 20 LTS 或更新版本。预检失败会立即终止，不停止现有服务，也不会进入版本切换。

### P2：可观测性与回滚

1. 将部署阶段明确拆分为：上传、解包、依赖、停止服务、切换、启动、健康检查。
2. 每个阶段记录开始和结束时间，并在失败时输出磁盘空间、Node 版本和当前 release 目录。
3. 健康检查必须校验 `buildSha`，不能只检查 HTTP 200；HTTP 200 但仍是旧版本时应标记发布未完成。
4. 保留失败 release 的错误摘要，但不保留完整的巨大 `node_modules` 副本。

## 五、发布验收清单

- [x] `npm run lint` 通过
- [x] `npm run build` 通过
- [x] 服务器 Node.js 版本达到 20 LTS
- [x] 服务器可用磁盘空间通过预检
- [ ] `npm ci --omit=dev` 在服务器成功
- [x] `node -e "require('better-sqlite3')"` 成功（生产服务已加载数据库）
- [x] systemd 服务启动成功
- [x] `curl http://127.0.0.1:8787/api/health` 返回 `ok: true`
- [ ] 公网 `/api/health` 的 `buildSha` 等于本次提交 SHA
- [ ] 登录、公司 AI 配置、AI 调试按钮和语音转文字流程人工验收

## 六、当前结论

服务器磁盘清理、Node.js 20 升级、原生 SQLite 依赖重编译和旧版本服务恢复已经完成；本机及公网健康接口均正常。最新功能仍未发布，下一步是安装新的受限部署脚本、重新触发 `Deploy production`，并以公网 `buildSha` 验证为最终发布依据。
