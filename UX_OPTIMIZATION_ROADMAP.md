# 智建协同 Pro 用户体验优化与开发路线

> 文档目的：把用户视角下发现的问题整理成可执行的后续开发清单，供产品、设计和开发按阶段推进。
>
> 评估基线：已检查桌面端 1280×720 和移动端 390×844 的首页、导航、项目看板、新建项目和移动工作台流程。
>
> 当前判断：产品已具备内部试用的功能骨架，但在数据可信度、保存同步反馈和核心任务闭环方面仍需优先完善，暂不建议直接规模化推广。

## 一、优化目标

最终希望新用户能够在 10 分钟内完成以下闭环：

1. 创建一个项目。
2. 为项目安排一项任务并指定负责人。
3. 在手机端完成一次现场勘察或上传一份项目资料。
4. 明确看到数据已保存、是否已同步，以及失败时如何重试。
5. 从首页风险或待办入口直接进入处理页面，并完成处理。

## 二、问题优先级定义

- **P0：阻断使用或破坏信任**。影响数据正确性、保存结果、核心流程，必须优先修复。
- **P1：明显降低效率**。用户可以完成工作，但需要绕路、重复查找或理解成本较高。
- **P2：体验和运营完善**。不阻断主流程，但影响长期使用、培训和规模推广。

## 三、P0：上线前必须完成

### P0-1 统一项目和业务数据口径

**现象**

- 首页可以显示风险、人员、资金和工作群动态，但项目看板可能显示 0 个项目。
- 手机端部分模块存在内置示例数据，桌面端则可能显示空状态。
- 项目、任务、合同、材料、外协单位之间的关联不总是可追溯。

**用户影响**

用户无法判断哪些是真实业务数据，容易认为系统统计不准确。

**开发任务**

- 明确“演示数据”和“真实数据”的边界，并提供显式的演示模式开关。
- 移除移动端各模块独立的默认业务种子数据，统一从同一数据层读取。
- 首页所有数字都从统一数据源计算，并显示数据更新时间或数据范围。
- 风险、任务、材料、合同、人员均应携带稳定的项目 ID，避免只用名称匹配。
- 为数据迁移增加版本号和兼容策略。

**验收标准**

- 新帐号首次进入时所有业务模块数据口径一致。
- 同一项目在桌面端和移动端的名称、阶段、任务数、风险数一致。
- 首页的项目总数与项目看板实际项目数一致。
- 示例数据不会混入正式帐号，且用户可以明确知道当前是否处于演示模式。

### P0-2 修复空工作区新建项目流程

**现象**

新工作区的看板初始结构为空，而新建项目逻辑依赖已有阶段列；可能出现界面提示创建成功，但项目没有真正进入看板。

**开发任务**

- 把生命周期阶段列作为结构初始化数据，而不是业务项目种子数据。
- 创建项目时保证阶段结构存在；不存在时先初始化再写入项目。
- 创建成功后重新读取服务端数据，避免只依赖本地乐观更新。
- 创建失败时不得显示“新建项目成功”，应显示明确的失败原因和重试按钮。
- 补充空工作区的自动化测试。

**验收标准**

- 新帐号从空白状态创建项目后，项目立即出现在看板、生命周期、首页和移动端。
- 刷新页面、重新登录后项目仍然存在。
- 后端不可用时，界面明确显示“已保存到本地，等待同步”，而不是伪装成已完成。

### P0-3 增加可见的保存、同步和冲突状态

**现象**

系统已有离线队列和同步逻辑，但普通页面看不到保存状态；同步失败、冲突和后端不可用时反馈不足。

**开发任务**

- 在顶部或移动端顶部增加统一状态：已保存、正在同步、离线、同步失败、存在冲突。
- 对上传、编辑、删除等操作显示处理中、成功和失败状态。
- 提供同步详情面板：失败记录、失败原因、最后同步时间、手动重试。
- 冲突数据提供“保留本机、采用服务器、查看差异”操作。
- 区分“本地保存成功”和“服务端同步成功”。

**验收标准**

- 断网后进行编辑，用户能看到离线状态和待同步数量。
- 恢复网络后能看到同步进度和最终结果。
- 同步失败不会静默消失，用户可以重试。
- 任何会造成数据丢失的冲突都必须有可见提示。

### P0-4 打通首页指标和风险处理闭环

**现象**

- “待审批/待确认”数字点击后不能直接进入对应处理清单。
- 风险项通常只能跳到整个模块，无法自动定位到具体项目、任务或材料。

**开发任务**

- 每个首页指标绑定明确的目标列表和筛选条件。
- 风险项跳转时携带项目 ID、记录 ID 和筛选条件。
- 提供快速处理动作：指派负责人、调整截止日期、补资料、提交审批、确认完成。
- 处理后返回原上下文，并更新首页数字。

**验收标准**

- 点击“逾期任务”只显示逾期任务。
- 点击某条材料到货风险后，直接打开对应材料或订单详情。
- 点击“待审批”可以逐条完成审批，不需要用户再次搜索。
- 完成处理后首页统计在合理时间内更新。

### P0-5 补齐协作附件和现场凭证闭环

**现象**

移动工作群的附件入口仍提示“即将接入”，但工程现场沟通高度依赖图片、图纸、验收凭证和文件。

**开发任务**

- 支持图片、PDF、常见办公文件上传。
- 上传前显示文件名、大小、缩略图和删除操作。
- 上传中显示进度，失败后支持重试。
- 文件与项目、群组、任务建立关联。
- 附件支持预览、下载和权限控制。

**验收标准**

- 手机端可以拍照并发布到指定项目群组。
- 网络中断后不会丢失已选择的附件。
- 其他成员能看到附件名称、上传人、时间和关联项目。

## 四、P1：核心效率优化

### P1-1 增加首次使用向导和空状态引导

- 为新用户提供 3—5 步初始化向导：创建项目、导入任务、添加人员、配置组织、开始勘察。
- 空看板不要只显示“00”，应显示下一步操作和示例说明。
- 提供 Excel 模板下载和导入校验结果。
- 提供“加载演示项目”和“清除演示数据”入口。

**验收标准**：新用户不看说明文档也能完成首次项目初始化。

### P1-2 全局搜索直达实体详情

- 项目搜索结果直接进入项目详情，而不是只进入项目看板。
- 人员、材料、合同、任务、外协单位统一纳入搜索范围。
- 搜索结果显示类型、项目、状态和最近更新时间。
- 支持无结果建议和搜索历史。

**验收标准**：用户从搜索框输入一个项目名，最多一次点击打开目标项目详情。

### P1-3 支持浏览器返回、深链接和页面恢复

- 当前模块、项目 ID、筛选条件和分页信息写入 URL。
- 浏览器返回可以回到上一个模块或项目详情。
- 分享链接后，具备权限的用户可以直接打开对应项目或任务。
- 刷新页面后保留当前上下文。

### P1-4 精简导航并按角色组织工作区

当前功能入口较多，建议提供角色化工作区：

- **现场执行**：现场勘察、今日待办、资料上传、验收。
- **项目经理**：项目汇总、生命周期、风险、任务、协作。
- **采购财务**：合同、材料、订单、成本预算。
- **系统管理**：组织、帐号权限、设置、版本管理。

低频模块可以折叠到“更多功能”，减少首次使用时的认知负担。

### P1-5 重新定义“快速保存”和配置导入导出

- 如果只是记录当前时间，应改为“记录保存点”。
- 如果保留“快速保存”，必须提供保存版本列表和恢复功能。
- 导入配置前显示将覆盖的内容，并支持预览差异。
- 导入失败时指出具体字段和行号。
- 导出文件应显示导出时间、帐号、数据范围和版本。

### P1-6 完善表单、删除和高风险操作反馈

- 所有日期、金额、负责人等字段补充可读标签和必填标识。
- 实时校验格式、范围和重复项。
- 保存按钮显示“保存中”，避免重复提交。
- 删除、重置、停用帐号时说明影响范围。
- 对重要操作提供撤销或回收站机制。

## 五、P2：长期体验和运营完善

### P2-1 建立通知中心

- 统一管理逾期、待反馈、待审批、@我、资料缺失和同步失败。
- 支持按项目、类型和时间筛选。
- 支持已读、全部已读和免打扰设置。
- 企业微信通知与站内通知使用同一事件记录。

### P2-2 增加帮助中心和业务说明

建议补充以下内容：

- 角色权限说明。
- 生命周期阶段流转规则。
- 资料命名和归档规则。
- 任务状态和验收状态说明。
- 离线使用限制。
- 常见错误与处理方式。
- 管理员部署、备份和恢复说明。

### P2-3 优化桌面端布局和图表

- 低高度屏幕下优化侧边栏和底部帐号区域。
- 隐藏模块不要初始化图表，避免尺寸为 0 的运行时警告。
- 图表筛选器必须与数据联动，避免出现看似可选但不改变结果的控件。
- 提供信息密度或紧凑模式。

### P2-4 建立可访问性和质量基线

- 键盘可完成主要流程。
- 所有图标按钮提供可理解的 aria-label。
- 颜色不是唯一的状态表达方式。
- 触控目标满足移动端最小尺寸。
- 建立端到端测试：登录、建项目、建任务、上传资料、离线编辑、同步恢复。

## 六、建议开发节奏

### 第 1 个迭代：数据可信与保存安全

- P0-1 统一数据口径。
- P0-2 修复空工作区建项目。
- P0-3 同步状态和失败重试。
- 增加关键数据自动化测试。

### 第 2 个迭代：核心工作闭环

- P0-4 首页指标直达处理。
- P0-5 协作附件和现场凭证。
- P1-1 首次使用向导和空状态。

### 第 3 个迭代：效率提升

- P1-2 全局搜索直达实体。
- P1-3 URL、返回和页面恢复。
- P1-4 角色化导航。
- P1-5 配置保存与导入导出重构。

### 第 4 个迭代：规模化运营

- P2-1 通知中心。
- P2-2 帮助中心。
- P2-3 桌面端布局和图表优化。
- P2-4 可访问性与端到端回归测试。

## 七、每个功能上线前的通用验收清单

- 空数据、正常数据、异常数据都能正确展示。
- 桌面端和移动端数据一致。
- 成功、失败、离线、同步中状态均有反馈。
- 刷新、返回、重复点击不会造成数据丢失或重复提交。
- 权限不足时有明确说明，并提供可行的下一步。
- 删除、重置、导入等高风险操作可确认、可追溯。
- 关键操作有操作人、时间和项目关联记录。
- 新用户不依赖开发人员口头说明即可完成主流程。

## 八、建议优先关注的用户指标

- 首次创建项目成功率。
- 首次项目初始化完成时间。
- 首页风险到处理完成的平均点击次数。
- 任务从创建到确认完成的闭环率。
- 现场资料上传成功率和失败重试率。
- 离线编辑同步成功率。
- 用户每周主动使用的模块数量。
- 新用户 7 日留存率。

## 九、第一阶段实施记录（2026-08-16）

已完成：

- 阶段列与项目业务数据分离；新工作区始终保留 9 个空阶段列。
- 移动端合同、日程、材料、订单、成本、人员和外协模块不再使用独立业务示例种子。
- 空业务数据接口改为可识别的空结果，避免前端用 404 表示正常空状态。
- 过滤已被删除工作区键的历史同步事件，避免旧任务、风险和示例数据回流。
- 启动时不再无条件清除 IndexedDB 业务缓存和待同步队列，保护用户本地工作。
- 顶部增加已同步、保存中、同步中、离线、同步失败和冲突状态提示。
- 首页工作台指标可点击进入对应处理模块，待审批按数据类型优先进入合同、供应链或成本模块。
- 桌面端和移动端空项目状态增加“创建第一个项目”的下一步引导。
- 新增同步中心，可查看待同步队列、同步失败原因和本地冲突，并支持重试、采用服务器版本或保留本机版本重试。
- 首页风险卡片可携带项目、任务、订单、成本或人员上下文；任务风险进入日程后自动展开项目、滚动定位并高亮具体任务。
- 供应链订单、成本项目、人员和外协风险进入对应模块后，会自动应用记录筛选并滚动高亮目标行。
- 合同风险进入合同管理后会自动按项目筛选；若携带合同编号，还会搜索、滚动并高亮对应合同。
- 移动端协作页移除示例群组和示例动态，空工作区显示明确的“暂无项目群组”引导，并处理异步群组加载后的默认选择。
- 已通过 `npm run lint` 和 `npm run build`。

待在第一阶段继续补充：

- 新建项目提交后的自动化回归测试和失败重试交互。
- 同步冲突详情页，以及“保留本机/采用服务器”处理动作。
- 风险直达闭环已完成第一版；下一步转向断网恢复、重复提交和服务不可用的端到端回归测试。
- 断网、恢复网络、重复提交和服务端不可用场景的端到端测试。

## 十、第二阶段实施记录（2026-08-17）

已完成第一版：

- 搜索结果覆盖项目、人员、材料、合同、供应链订单和外协单位；项目结果可直接打开项目详情。
- 当前模块、项目 ID、供应链子页写入 URL，支持浏览器返回和刷新恢复主要页面上下文。
- 移动端协作支持图片、PDF、Office 等附件选择，限制大小和数量，发布前可移除，发布后可下载并预览图片。
- 新增首次使用向导，覆盖创建项目、安排任务、添加人员三个初始化步骤。
- 移动协作空状态不再展示演示群组和动态，改为明确的项目群组引导。
- 首页任务风险增加“调整截止日期”和“标记完成”快捷动作，处理后通过同步数据更新风险状态。
- 未分配负责人风险增加“指派负责人”，输入姓名或协作单位后直接写回任务。
- 材料到货风险增加“标记已到货”，安全培训风险增加“标记已培训”，处理后分别更新订单和人员数据。
- 合同缺失风险增加“新建合同”，进入合同表单时自动带入关联项目。
- 外协资料缺失支持按资料项选择文件上传，上传成功后自动标记对应资料，并记录文件地址和上传时间。
- 待签批合同增加“审批通过”动作，保留审批时间并更新合同状态。
- 风险动作事件统一抽取为共享类型和派发工具，减少桌面端、移动端重复逻辑。
- 聊天、移动协作和外协资料上传统一使用共享文件上传工具，集中处理 8MB 限制、服务端上传和本地回退。
- 桌面端、移动端和外协资料上传增加上传中状态，并在上传期间禁止重复提交，避免慢网络下重复操作。
- 全局搜索补充任务记录、无结果提示和最近搜索历史，并继续复用统一风险定位事件。
- 桌面端和移动端首页新增版本更新卡片，展示当前版本、更新摘要、日期，并可进入完整版本记录。
- 多项目看板改为响应式阶段网格，在桌面端同页展示各阶段，避免必须左右拖动查看。
- 施工日程新增按项目的计划与现场目标摘要，支持记录目标日期、偏差天数、偏差原因和新增/调整目标。
- 成本统计按生命周期阶段过滤，初期现场勘察和前期项目不再被误判为已发生成本。
- 供应链订单详情按钮接入真实详情弹窗，展示订单完整信息。
- 全生命周期与项目资料新增阶段筛选（项目立项、初步设计、商务沟通、签订合同），项目数量较多时可快速定位当前阶段。
- 项目看板、生命周期、项目资料和项目详情统一显示独立项目编号；兼容历史项目并为新项目预留 `projectNumber` 字段。
- 现场勘察档案支持单条删除和同一项目多条电房档案合并；合并时保留全部照片，并在备注中记录原电房名称。
- 项目资料页改为显眼的项目卡片选择区，支持项目编号/名称搜索与阶段筛选；归档位置可在当前页面直接设置并保存，不再依赖跳转系统设置。
- 本地桌面服务新增“打开本地文件夹”，可直接在系统文件管理器中打开当前归档根目录。
- 项目验收新增项目、阶段性/总验收和验收分项结构，内置结构、隐蔽、支架等验收类别，支持自定义分项、批量上传照片/文档并按分项归档。
- 合同管理新增项目与合同类别双重筛选、模板标识、合同文件上传、版本/修改记录和最终归档动作。
- 材料模块与供应链订单模块重新分工：材料模块突出库存、材料台账和项目 BOM；供应链模块突出采购订单、供应商和价格追踪。采购单支持绑定项目编号与 BOM 清单，采购数量自动回写实际采购量。
- 成本台账新增采购人工/装卸费与辅料费用类型，并将供应链采购订单自动同步为项目预计/实际材料支出。
- 工作群支持删除案例/测试群及其动态，并可为群组配置飞书文档、飞书群聊和企业微信群聊的站外链接；链接可在群头部直接打开或维护。
- 参建外协新增编辑/删除入口，保留已上传资料；施工日程任务新增名称、负责人修改和删除入口，删除任务时会解除其后续任务的前置依赖。
- 材料库存新增材料类别、规格、库存数量、存放位置等字段的编辑和删除入口；删除时同步清理对应入库流水。
- 配置导入增加导入类别预览和覆盖确认，避免用户误覆盖当前工作区。

仍需继续：

- 首页风险卡片仍需补齐人员指派；附件还需补充上传进度、断点续传和更细的权限策略。
- 附件真正的服务端文件存储、上传进度、断点续传和权限控制。
- 搜索结果补齐任务、成本和更多材料字段，并增加无结果建议和搜索历史。
- 导入配置前的差异预览、覆盖确认和字段级错误提示。
